# check if id is present in database before taking pictures
# check if yml data path is present or not and respond accordingly.
# check if full name contain both name and surname of length 2. (done)
# check if attendance is already taken or not.
# delete images if student_info is removed or updated(logic unknown).
